#include<stdio.h>
int main()
{
	int no;
	
	for
	{
		{
		if(no%2==0)
		esum=esum+no;
    	}
	else
	{
		osum=osum+no;
	}
	no++;
    }
    while(no<=100);
    printf("\n esum=%d",esum);
    printf("\n osum=%d",osum);
}
