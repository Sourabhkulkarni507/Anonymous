#include<iostream>
using namespace std;
int main()
{ 
   int a,b,c;
   cout<<"\nEnter the angles of the triangle in degree  : ";
   cin>>a>>b>>c;
   if(a+b+c = 180)
   {
   	cout<<"\nthe given triangle is valid .";
   }
   else
   {
   	cout<<"\nThe given triangle is not valid .";
   }
}
